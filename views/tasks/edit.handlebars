<div class="container-inputs">
  <h1>Editando a task: {{task.title}}</h1>
  <form action="/tasks/edit" method="POST">
    <input  type="hidden" name="id" value="{{task.id}}">
    <div class="form-control">
      <input class="forms" type="text" name="title" placeholder="O que você vai fazer?" value="{{task.title}}">
    </div>
    <div class="form-control">
      <textarea class="forms form-area" type="text" name="description" placeholder="Descreva a tarefa...">{{task.description}}</textarea>
    </div>
    <input class="btn-primary" type="submit" value="Editar">
  </form>
</div>